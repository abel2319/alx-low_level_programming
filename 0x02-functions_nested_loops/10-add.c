#include <stdio.h>
#include "main.h"

/**
*add - Entry to code
*
* @a : first integer
* @b : second integer
*
* Return: Always the sum of inputs (Success)
*/
int add(int a, int b)
{
	return (a + b);
}
